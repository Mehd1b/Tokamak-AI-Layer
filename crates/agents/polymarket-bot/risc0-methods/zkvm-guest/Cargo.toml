[package]
name = "polymarket-bot-guest"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
polymarket-bot = { path = "../../agent" }
kernel-guest = { path = "../../../../runtime/kernel-guest" }
risc0-zkvm = { version = "0.22", default-features = false, features = ["guest"] }
