# Build
CLAUDE.md
target/
*/target/

# Local config
/.claude
/prompts
/bundles/*
/run/*
/content

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local
.env.prod
.env.staging
# tokagent-billing-server Fly secrets — only the .example template is tracked
tokagentos/scripts/billing-server/.env.prod
tokagentos/scripts/billing-server/.env.staging

# Worktrees
.worktrees/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Coverage
coverage/
lcov.info

# Generated agent-pack manifests (keep only the example)
dist/agent-pack.json

# Frontend build artifacts
frontend/.next/
frontend/node_modules/
frontend/.env.local
frontend/data/

# superpowers planning artifacts (brainstorm/plan files, not product code)
.superpowers/
**/.superpowers/

# tokagentos: native-plugins were intentionally removed in cleanup (Task #11).
# Don't re-track if they reappear locally (e.g. via a stale checkout).
tokagentos/packages/native-plugins/
