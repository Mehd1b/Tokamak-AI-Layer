# ═══════════════════════════════════════════════════════════
# Trading Bot — Environment Configuration
# ═══════════════════════════════════════════════════════════
#
# 1. Copy this file:  cp .env.example .env
# 2. Fill in YOUR_KEY values below
# 3. Run the bot:     npm start
#

# ── Network ────────────────────────────────────────────────
# Your Ethereum RPC URL (Alchemy, Infura, QuickNode, etc.)
ETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# ── Wallet ─────────────────────────────────────────────────
# Your wallet private key — NEVER share or commit this
PRIVATE_KEY=0x...

# ── Strategy (auto-configured) ─────────────────────────────
STRATEGY_ID=__STRATEGY_ID__
STRATEGY_MODE=__STRATEGY_MODE__

# JSON array of trades to execute
# Each entry: { action, tokenIn, tokenOut, amountIn, minAmountOut, poolFee, route }
TRADES=__TRADES__

# ── Risk Management ────────────────────────────────────────
# Price thresholds (in wei) for automated exit
STOP_LOSS_PRICE=__STOP_LOSS_PRICE__
TAKE_PROFIT_PRICE=__TAKE_PROFIT_PRICE__

# Maximum slippage tolerance in basis points (100 = 1%)
MAX_SLIPPAGE_BPS=100

# ── Auto-Execution Listener ───────────────────────────────
# Set to false to only log triggers without executing exits
AUTO_EXECUTE=true

# Trailing stop: sell if portfolio drops this % from its peak
TRAILING_STOP_PERCENT=__TRAILING_STOP_PERCENT__

# How often (in seconds) the listener checks positions
CHECK_INTERVAL=60

# ── Investment Mode (optional) ─────────────────────────────
# Only relevant when STRATEGY_MODE=investment
#
# ENTRY_STRATEGY=lump-sum
# ALLOCATIONS=[{"tokenAddress":"0x...","symbol":"USDC","targetPercent":50}]
#
# ── DCA Schedule (when ENTRY_STRATEGY=dca) ─────────────────
# DCA_FREQUENCY=weekly
# DCA_TOTAL_PERIODS=52
# DCA_AMOUNT_PER_PERIOD_PERCENT=2
#
# ── Rebalancing ────────────────────────────────────────────
# REBALANCE_TYPE=drift
# REBALANCE_FREQUENCY=monthly
# DRIFT_THRESHOLD_PERCENT=5
#
# ── Exit Criteria ──────────────────────────────────────────
# EXIT_TAKE_PROFIT_PERCENT=50
# EXIT_STOP_LOSS_PERCENT=20
# EXIT_TIME_MONTHS=12
