# Next.js Environment Variables

# WalletConnect Project ID (required for RainbowKit)
# Get one at: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLET_CONNECT_ID=your_project_id_here

# Thanos Sepolia RPC URL (server-side, used for agent resolution)
THANOS_RPC_URL=https://rpc.thanos-sepolia.tokamak.network

# Pinata IPFS API Keys (required for agent registration)
# Get these at: https://app.pinata.cloud/
PINATA_API_KEY=your_pinata_api_key_here
PINATA_SECRET_KEY=your_pinata_secret_key_here

# Agent Runtime URL - fallback when on-chain IPFS resolution fails
# Points to the local agent-runtime Express server
AGENT_RUNTIME_URL=http://localhost:3001
