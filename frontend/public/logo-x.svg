<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 800" width="800" height="800">
  <!-- Background -->
  <rect width="800" height="800" rx="0" fill="#0a0a0f"/>
  <!-- Outer diamond (kernel) -->
  <path d="M400 160 L600 360 L400 560 L200 360 Z" fill="none" stroke="#A855F7" stroke-width="12" stroke-opacity="0.7" stroke-linejoin="round"/>
  <!-- Inner diamond (offset, execution layer) -->
  <path d="M400 280 L540 400 L400 520 L260 400 Z" fill="none" stroke="#A855F7" stroke-width="10" stroke-opacity="0.4" stroke-linejoin="round"/>
  <!-- Core (agent) -->
  <circle cx="400" cy="400" r="28" fill="#A855F7"/>
</svg>
